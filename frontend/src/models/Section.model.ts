export interface SectionData {
    _id: string;
    name: string;
    startDate: string;
    endDate: string;
    referenceProjectId: string;
    description: string;
}

export interface Section {
    id: string;
    name: string;
}