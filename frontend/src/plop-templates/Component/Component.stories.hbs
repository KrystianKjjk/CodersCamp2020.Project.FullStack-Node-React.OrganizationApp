import React from 'react';
import { Story, Meta } from '@storybook/react/types-6-0';
import {{pascalCase name}} , { {{pascalCase name}}Props } from './{{pascalCase name}}';
import { store } from '../../app/store';
import { Provider } from 'react-redux';

export default {
  title: '{{pascalCase name}} component',
  component: {{pascalCase name}},
} as Meta;

const Template: Story<{{pascalCase name}}Props> = (args) => (
  <Provider store={store}>
    <{{pascalCase name}} {...args} />
  </Provider>
);

export const Sample{{pascalCase name}} = Template.bind({});
Sample{{pascalCase name}}.args = {
  name: '{{pascalCase name}}',
};